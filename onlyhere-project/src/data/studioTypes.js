// ── DELETED: this was a second copy of the draft prompts ─────────────
// Found 10 Aug 2026 while tracing why a live page still showed "Why People
// Love It". This file held a byte-level fossil of the eight Studio draft
// prompts, imported by NOTHING, still carrying the headings the 8 Aug
// entry-voice pass removed. Fourth duplicated thing found in one day, after
// resolveLegMode, lookupRealPlace and the two heading lists.
//
// The live prompts are in src/utils/studioPrompts.js and nowhere else. A test
// asserts that the prompt text appears in exactly one file, so this cannot come
// back quietly.
//
// The file is left here as an empty marker only because the Cowork session that
// emptied it can write to disk but cannot delete. Safe to `git rm` at any time.
export {};
